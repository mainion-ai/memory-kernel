# Triage routing for memory-kernel.
#
# Lines later in this file override earlier ones. Wildcards match
# unless a more specific rule applies. See GitHub's CODEOWNERS docs:
# https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-configuration/customizing-your-repository/about-code-owners
#
# Routes to the two maintainers (@NePav, @mainion-taj) directly
# rather than to a team. `mainion-ai` is a personal account, not a
# GitHub Organization, so teams aren't available. If `mainion-ai`
# is later converted to an org (or repos move to a new org), switch
# this to:
#   *       @<org>/maintainers
# and add both users as members of that team. Conversion decision
# is out of scope for #199.
#
# Per-directory routing (e.g. packages/openclaw-memory-kernel/ to
# a separate owner) is a future addition; tracked in #199 part (d)
# (triage doctrine).

*       @NePav @mainion-taj
